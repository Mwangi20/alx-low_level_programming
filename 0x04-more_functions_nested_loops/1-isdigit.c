#include "main.h"

/**
 * _isdigit - function that checks for a digit
 * @c: number to be checked
 * Return: 1 if is digit 0 any else
 */

int _isdigit(int c)
{
	if (x >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
